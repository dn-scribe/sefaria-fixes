name: Sync to Hugging Face

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  sync-to-huggingface:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true

      - name: Push to Hugging Face Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          
          # Remove large data files before pushing
          git rm -r --cached data/ || true
          git rm --cached tmp_lh_llm.json || true
          git rm --cached tmp_lh_links.json.backup || true
          git rm --cached tmp_lh_links.csv || true
          git rm --cached *.csv || true
          git commit -m "Remove large data files for HF deployment" || true
          
          # Add Hugging Face remote
          git remote add hf https://huggingface.co/spaces/dn-9281411/linking-lh || true
          
          # Push to Hugging Face
          git push https://user:$HF_TOKEN@huggingface.co/spaces/dn-9281411/linking-lh main:main --force
